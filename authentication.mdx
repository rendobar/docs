---
title: "Authentication"
description: "How to authenticate with the Rendobar API using session cookies or API keys"
sidebarTitle: "Authentication"
icon: "key"
---

Authenticate with a session cookie (dashboard) or an API key (programmatic). Keys carry the `rb_` prefix and go in the `Authorization` header as a Bearer token.

## Session-based auth (Dashboard)

Sign in at [app.rendobar.com](https://app.rendobar.com) and the dashboard sets an `rb_session` cookie automatically. It lasts 30 days and covers every API call from the dashboard — no extra setup.

## API key auth (Programmatic)

For programmatic access, create an API key in the dashboard under **Settings > API Keys**. API keys start with the `rb_` prefix.

Include the key in the `Authorization` header:

<CodeGroup>

```bash cURL
curl https://api.rendobar.com/jobs \
  -H "Authorization: Bearer rb_live_YOUR_API_KEY"
```

```javascript JavaScript
const response = await fetch("https://api.rendobar.com/jobs", {
  headers: {
    "Authorization": "Bearer rb_live_YOUR_API_KEY",
  },
});
```

```python Python
import requests

response = requests.get(
    "https://api.rendobar.com/jobs",
    headers={"Authorization": "Bearer rb_live_YOUR_API_KEY"},
)
```

</CodeGroup>

### API key properties

- Keys are scoped to an organization, not a user
- Each key has a name for identification
- Keys can be revoked at any time from the dashboard
- Revoked keys return `401 Unauthorized` immediately
- There is no limit on the number of keys per organization

### Creating a key

```bash
curl -X POST https://api.rendobar.com/api-keys \
  -H "Authorization: Bearer rb_live_EXISTING_KEY" \
  -H "Content-Type: application/json" \
  -d '{ "name": "CI Pipeline" }'
```

### Revoking a key

```bash
curl -X DELETE https://api.rendobar.com/api-keys/KEY_ID \
  -H "Authorization: Bearer rb_live_YOUR_KEY"
```

## Auth context

Every authenticated request resolves to a context containing:

- **userId** -- the authenticated user
- **orgId** -- the active organization
- **plan** -- the organization's current plan (determines rate limits, allowed job types, and credit balance)

This context is used for rate limiting, plan enforcement, and credit checks on every request.

## MCP agent auth

AI agents can authenticate with Rendobar via the Model Context Protocol (MCP). The primary method is API key authentication -- the same keys used for the REST API work with MCP by passing them in the `Authorization` header.

For MCP clients that support it, Rendobar also offers OAuth 2.1 via the Better Auth OAuth Provider plugin with dynamic client registration, PKCE, and org-scoped tokens. See the [MCP overview](/mcp/overview) for setup details.

## Error responses

Authentication failures return:

```json
{
  "error": {
    "code": "UNAUTHORIZED",
    "message": "Invalid or expired authentication"
  }
}
```

HTTP status: `401`

<CardGroup cols={2}>

<Card title="Quickstart" icon="rocket" href="/quickstart">
  Make your first API call
</Card>

<Card title="Credits" icon="coins" href="/credits">
  Billing and usage
</Card>

<Card title="Raw FFmpeg" icon="terminal" href="/job-types/raw-ffmpeg">
  All available operations
</Card>

<Card title="CLI" icon="terminal" href="/cli/authentication">
  Sign in from the terminal
</Card>

<Card title="MCP" icon="robot" href="/mcp/overview">
  AI agent integration
</Card>

</CardGroup>
