<Warning>
Errors come back as `{ error: { code, message } }`. Always check before reading `data`:

```json
{ "error": { "code": "INSUFFICIENT_CREDITS", "message": "Not enough credits" } }
```

See the full [error code reference](/support/errors) for every code you can get back.
</Warning>
