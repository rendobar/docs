<Warning>
Always check the response for errors:

```json
{ "error": { "code": "INSUFFICIENT_CREDITS", "message": "Not enough credits" } }
```

Common codes: `UNAUTHORIZED` (401), `VALIDATION_ERROR` (400), `INSUFFICIENT_CREDITS` (402), `RATE_LIMITED` (429).
</Warning>
