---
title: "Animation asset reference"
sidebarTitle: "animation fields"
description: "Add a Lottie animation exported by a designer with its transparency kept, and every field the asset accepts."
icon: "bolt"
keywords: ["lottie video api", "animated overlay api", "json animation video"]
canonical: "https://rendobar.com/docs/jobs/compose/reference/animation"
---

{/* GENERATED by scripts/gen-docs-compose-reference.mts. Do not edit. */}

Set `asset.type` to `"animation"`. These are its own fields, on top of the [clip fields](/docs/jobs/compose/reference/timeline#clip) every clip takes.

| Field | Type | What it does |
| --- | --- | --- |
| `src` * | string | Lottie animation URL: the .json file LottieFiles, After Effects (Bodymovin) and Figma plugins export. It plays once, for its own length, with a transparent background, and fits the frame like an image: place and size it with the transform. |
| `trim` | object | Which part of the source file to use, in source seconds. Omit to play from the start. |
| `trim.from` * | number | Start second in the SOURCE file. min 0. |
| `trim.to` * | number | End second in the SOURCE file (exclusive). over 0. |

Fields marked `*` are required. Every table on this page is generated from the schema the API validates with, so it cannot drift from what a render accepts.
