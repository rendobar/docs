---
title: "Image asset reference"
sidebarTitle: "image"
description: "Add a still image clip and control how long it lasts and how it fills its box, with every field listed in full."
icon: "image"
keywords: ["image to video api", "still image clip", "photo video api"]
canonical: "https://rendobar.com/docs/jobs/compose/reference/image"
---

{/* GENERATED by scripts/gen-docs-compose-reference.mts. Do not edit. */}

Set `asset.type` to `"image"`. These are its own fields, on top of the [clip fields](/docs/jobs/compose/reference/timeline#clip) every clip takes.

| Field | Type | What it does |
| --- | --- | --- |
| `src` * | string | Image URL (png/jpg). Give a photo life with scale keyframes from 1 to about 1.15, a Ken Burns move. |

Fields marked `*` are required. Every table on this page is generated from the schema the API validates with, so it cannot drift from what a render accepts.
