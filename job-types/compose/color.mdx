---
title: "Compose color and filters"
sidebarTitle: "Color & filters"
description: "Tune color on a clip: grade for mood, lift flat or hazy footage, or apply a one-shot look like dramatic black and white."
icon: "palette"
tag: "Beta"
keywords: ["color grading api", "video filter", "black and white video", "compose"]
canonical: "https://rendobar.com/docs/job-types/compose/color"
---

Each effect below is a field on a clip. The source is on the left, the `compose` result on the right.

## Color grade

`color` grades a clip. Set `contrast`, `saturation`, `temperature`, `brightness`, and `gamma` to set a cinematic mood, or push them to lift flat, hazy footage.

```json
"color": { "contrast": 1.12, "saturation": 1.2, "temperature": 6800, "gamma": 0.95 }
```

<Frame caption="Cinematic color grade to set a mood">
  <video src="https://cdn.rendobar.com/assets/showcase/fx/color-grade.mp4" autoPlay loop muted playsInline />
</Frame>

<Frame caption="Lift flat or hazy footage">
  <video src="https://cdn.rendobar.com/assets/showcase/fx/social-pop.mp4" autoPlay loop muted playsInline />
</Frame>

## Filters

`filter` applies a one-shot look: `greyscale`, `negative`, `boost`, `muted`, `lighten`, `darken`, or `contrast`.

```json
"filter": "greyscale"
```

<Frame caption="Dramatic black and white (filter: greyscale)">
  <video src="https://cdn.rendobar.com/assets/showcase/fx/black-white.mp4" autoPlay loop muted playsInline />
</Frame>
